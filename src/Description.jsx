function Description(){
  let feature = "unbeleivable";
  return <h2>WE MAKE THE THINGS {feature.toUpperCase()} !</h2>;
}

// this is named export - this is used when we have multiple component on the same page
export {Description};